local import = require(game.ReplicatedStorage.Shared.packages.Import)

local Colors = import "Components/Theme/Colors"

local RSFLight = {
	Background = Colors.Grey200,
	Foreground = Colors.Grey900,

	Button = {
		Fill = Colors.Grey300
	}
}